随着序列的增长而相乘叠加，超空间上的细节叠加放大会越来越多，以至于局部会存在一些面积非常小但是值非常大的点，此时就存在梯度爆炸。我们通过截断来限制求梯度的超空间不叠加某个范围以前的细节，这样就可以保证梯度限制在正常的范围内。同时我们拒绝了过多的求幂运算

这时候我们损失了一定方向上的准确性，但是限制了不会在其方向上下降的太远，以至于超空间的点反向凸起（由于损失函数规定的是距离），当然也可能损失先前重要的叠加却因为截断消失

当然，损失函数相对于权重，输出，偏置的导数计算会有一定区别